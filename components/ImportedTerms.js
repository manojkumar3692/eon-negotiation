"use client";
export default function ImportedTerms({product,currency,onUseShipping}){
 const facts=product.commerceFacts||{},p=facts.pricing,t=facts.storeTerms;
 const money=n=>new Intl.NumberFormat('en',{style:'currency',currency}).format(n/100);
 const shipping=t?.shipping;
 const shippingLabel=!shipping||shipping.mode==='unknown'?'Needs confirmation':shipping.mode==='free'?'Always free to the customer':shipping.mode==='flat'?money(shipping.customerChargeMinor):shipping.mode==='threshold'?`Free from ${money(shipping.thresholdMinor)}; otherwise ${money(shipping.customerChargeMinor)}`:'Calculated for the customer’s destination';
 return <section className="imported-terms" aria-label="Imported store information"><h3>From your store</h3><p>These are store facts, not negotiation limits. Sync again when your store changes.</p><dl><div><dt>Regular / crossed-out price</dt><dd>{p?.regularMinor!=null?money(p.regularMinor):'Not supplied'}</dd></div><div><dt>Current selling price</dt><dd>{p?money(p.sellingMinor):'Needs confirmation'}</dd></div><div><dt>Customer shipping</dt><dd>{shippingLabel}</dd></div><div><dt>Coupons</dt><dd>{t?.promotions.status==='known'?(t.promotions.offers.length?`${t.promotions.offers.length} supplied`:'Store reports no offers'):'Needs confirmation'}</dd></div></dl>{!p&&<p role="status">Legacy imported amount: {money(product.priceMinor)}. The store has not identified whether this is its regular or selling price. Negotiation cannot launch until the connector supplies confirmed pricing.</p>}{p?.taxBasis==='exclusive'&&<p role="status">Tax-exclusive pricing needs a checkout tax integration before live negotiation. The current engine supports tax-inclusive prices.</p>}{p&&<small>Source: {p.source} · {p.taxBasis==='inclusive'?'Tax included':'Tax excluded'} · {facts.asOf?new Date(facts.asOf).toLocaleString():'Sync time unavailable'}</small>}{t?.promotions.offers.map(o=><p key={o.code}><strong>{o.code}</strong> — {o.description}<br/><small>Combines with negotiation: {o.combinesWithNegotiation}. {o.expiresAt?`Expires ${new Date(o.expiresAt).toLocaleString()}.`:''} Final eligibility and payable amount are checked for the actual cart. The current EON checkout does not stack coupons.</small></p>)}{shipping&&['free','flat','threshold'].includes(shipping.mode)&&onUseShipping&&<button type="button" className="secondary" onClick={()=>onUseShipping(shipping)}>Use imported shipping policy</button>}<p>Your product cost and protected minimum stay private and are never inferred from a coupon.</p></section>;
}
